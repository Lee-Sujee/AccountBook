<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ssafy.pjt.model.dao.UserDao">
	<insert id="insert" parameterType="User">
		INSERT INTO	users(user_id, user_password, user_name, user_age, user_job)
		VALUES(#{user_id}, #{user_password}, #{user_name}, #{user_age}, #{user_job})
	</insert>
	
	<select id="searchById" parameterType="String" resultType="User">
		SELECT * FROM users
		WHERE user_id = #{user_id}
	</select>
	
	<update id="update" parameterType="User">
		UPDATE user
		SET user_id = #{user_id}
			user_password = #{user_password}
			user_name = #{user_name}
			user_age = #{user_age}
			user_job = #{user_job}
		WHERE user_num = #{user_num}
	</update>
	
</mapper>
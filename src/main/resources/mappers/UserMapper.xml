<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ssafy.user.dao.UserDao">

	<insert id="insert" parameterType="SignUpRequestDto">
		INSERT INTO	users(email, password, name, age, job)
		VALUES(#{email}, #{password}, #{name}, #{age}, #{job})
	</insert>
	
	<select id="selectUserByEmail" parameterType="String" resultType="UserResponseDto">
		SELECT email, name, age, job
		FROM users
		WHERE email = #{email}
	</select>
	
	<update id="updateUserInfo" parameterType="UserUpdateRequestDto">
		UPDATE users
		SET name = #{name}
			age = #{age}
			job = #{job}
		WHERE id = #{id}
	</update>
	
</mapper>
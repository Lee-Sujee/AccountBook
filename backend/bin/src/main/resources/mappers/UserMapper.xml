<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ssafy.user.repository.UserRepository">

	<insert id="insertUser" parameterType="SignUpRequestDto">
		INSERT INTO	users(id, email, password, name, age, job)
		VALUES(#{id}, #{email}, #{password}, #{name}, #{age}, #{job})
	</insert>
	
	<select id="selectUserById" parameterType="String" resultType="User">
		SELECT *
		FROM users
		WHERE id = #{id}
	</select>
	<select id="selectUserByEmail" parameterType="String" resultType="User">
		SELECT *
		FROM users
		WHERE email = #{email}
	</select>
	
	<update id="updateUserInfo" parameterType="User">
		UPDATE users
		SET name = #{name},
			age = #{age},
			job = #{job}
		WHERE email = #{email}
	</update>
	
	<update id="updatePassword" parameterType="User">
		UPDATE users
		SET password = #{password}
		WHERE id = #{id}
	</update>
</mapper>